# 쇼핑쇼츠 자동화 — PRD (단일 출처)

> 이 문서가 규격의 **단일 출처(Single Source of Truth)**다. 스킬/스크립트는 이 문서를 따른다.
> 채널: https://www.youtube.com/channel/UCxQK4IBAJ1t-dPImmMinCng

---

## 0. 목표와 전략

**목표:** 이 채널에 쇼핑쇼츠를 꾸준히 업로드해 수익화.

**채널 데이터 기반 전략 (지난 28일 / 6개월):**

| 지표 | 값 | 방향 |
|---|---|---|
| 성별 | 남 74.6% / 여 25.4% | **남성향 제품** |
| 연령 | 25–34(37.2%) · 35–44(22.5%) · 45–54(18.2%) = 약 78% | **구매력 있는 25–54 남성** |
| 수익원 | 제휴 71,015원 ≫ Shorts광고 15,143원 ≫ 보기광고 24원 | **핵심은 제휴 전환** |
| 수익 추세 | 6월 24만 → 8월 2.4만 (하락) | 개편 필요 |

**핵심 결론:** 조회수(광고)보다 **제휴 전환**이 수익의 90%. 제품 선정 1순위 기준 = "25–54 남성이 실제 지갑 여는 + 유튜브 쇼핑으로 태그 가능한 제품".

**타겟 카테고리 (2026-08-22 확정):** **청소용품 · 생활용품** 소개 중심.
- **차량용품은 신규 제작에서 제외**한다(기존 제작분은 보존). 공구/DIY·가전소품·자기관리·캠핑은 부차 축으로만 유지.
- **특정 유통사(다이소·노브랜드·트레이더스)를 주제 축으로 삼지 않는다** (2026-08-22 결정). 이유: 유통사 PB 상품은 **유튜브 쇼핑 태그가 안 걸리는 경우**가 많아 제휴 전환(수익 90%)이 죽는다. 제품 자체로 승부하고, **쿠팡/스마트스토어에서 태그 가능한 제품만** 고른다.
  - 단, 해당 유통사에서 실제로 파는 제품이 후보로 올라오는 것 자체는 무방하다 — 태그 가능 여부만 확인하면 된다.
- 선정 기준 = **"이 고민이 있으면 이걸 사면 된다"가 20초 안에 성립하는 문제해결형 청소·생활용품."**
- **2026-08-16 확대 결정:** 기존 차량용품/남성용품 중심에서 **생활용품·청소용품으로 카테고리를 확대**한다. 남성 타겟 톤(§2 [1])은 유지하되, 소재 발굴 범위를 좁히지 않는다.

**수익화 방식(확정):** **유튜브 쇼핑 태그** — 각 쇼츠에 제품을 직접 태그한다.
- 태깅은 YouTube Studio에서 **수동**(공개 태깅 API 없음). 파이프라인은 "태그용 제품 정보 블록"(제품명·검색어·스토어 링크·가격)을 산출해 사람이 Studio에서 붙이게 한다.
- 전제: 채널이 **YouTube 쇼핑 제휴 프로그램** 자격을 갖춰야 함(이미 제휴 수익 발생 중이므로 충족 가정, 미충족 시 [7]에서 안내).

**⚠️ 저작권 리스크 (반드시 인지):** 틱톡 영상 재사용은 "재사용된 콘텐츠(reused content)"·저작권 정책에 걸릴 수 있음. 최근 수익 하락과 연관 가능성 있음. → [4] 편집에서 **컷 재배열 + 크롭/확대 + 타이틀/자막/TTS/BGM 레이어**로 최대한 변형을 준다. 완전 면제는 아님.

---

## 1. 실행 형태 (확정)

- **스킬 반자동** (`/쇼핑쇼츠`, 이름 확정 예정). 다른 프로젝트(scalemaker-yt, cardnews)와 동일 패턴.
- **사람이 컨펌하는 지점:** ① 제품 선정 ② 틱톡 소재 3개 최종 선택 ③ 대본 ④ 최종 영상 프리뷰 ⑤ 업로드 메타.
- **자동으로 도는 지점:** 편집(컷섞기·크롭·타이틀·자막 합성)·TTS·BGM·메타 초안.
- **틱톡 소재 확보:** Claude가 브라우저(chrome-mcp-stdio)로 틱톡에서 중국어 없는 후보를 탐색해 링크 제시 → 사람 확인 → 다운로드.

---

## 2. 파이프라인 (7단계)

```
[1] 제품 발굴 → [2] 소재 수집(틱톡 3개) → [3] 대본 → [4] 편집
     → [5] TTS(서현) → [6] BGM → [7] 업로드 준비(+쇼핑 태그 정보)
```

### [1] 제품 발굴  `pipeline/1_discover.py` + `pipeline/1_datalab_keywords.py` (+ 브라우저)
- **소스 A — 레퍼런스 채널 (2026-08-22 전면 교체):** 아래 채널들의 **쇼츠 조회수 10만+** 를 훑는다. 기존 차량용품 레퍼런스(쇼핑남자·남자마켓·hommster·살룸연구소·TJ홈스토어)는 **아카이브**(카테고리 전환으로 미사용).

  | 축 | 채널 | 핸들 | 참고 |
  |---|---|---|---|
  | 청소·생활 | 소핑소 | `@sopingso/shorts` | 954만/865만 — "다이소 직원들이 뽑은 화장실 청소템 Top5". 이 채널이 **1순위 벤치마크** |
  | 생활·살림 | 살림해커 | `@살림해커/shorts` | 쇼츠 48개 중 10만+ 23개. 다이소·코스트코 TOP N 포맷 양산형 |
  | 생활·가성비 | 득템 | `@duktem/shorts` | 10만+ 32개. 노브랜드 499만/422만. 가성비·생활비 훅의 교과서 |
  | 생활·살림 | 살림토끼 | `@sallim_rabbit/shorts` | 10만+ 20개. 다이소·노브랜드 양쪽 커버 |
  | 생활·셀프케어 | 또템 | `@ddo-tem/shorts` | 10만+ 14개. 다이소 코너별(뷰티·식품) 세분 포맷 |
  | 청소 전문 | 청소명장 박보성 | `@cleaning_pro_park/shorts` | 전문가 톤. **제품보다 "방법+제품" 훅** 참고 |
  | 자취·청소 | 단순할 현 | `@dansunhyun/shorts` | 기존 유지. "6년차 혼자 살면서 잘 쓰는 청소템" 시리즈 |
  | 자취·청소 | 자취남 | `@jachinam/shorts` | "화장실 곰팡이, 이걸로 지웠어요" 535만 |
  | 생활 활용법 | 오!득템 | `@오득템404/shorts` | 활용법 반전 포맷(132만). 편차 큼 — 상위편만 참고 |
  | 홈인테리어·집수리 | 랄리나홈 | `@랄리나홈/shorts` (Lallina_home) | 2026-08-31 추가. 150만("요즘 신축엔 이게 유행이래요")·140만·70.2만·63만. 셀프시공·수납·집수리 꿀템 축 — **"요즘 ~는 이게 유행이래요" 트렌드 훅** 참고 |

  - **관찰된 승리 포맷(2026-08-22 조사):** ① "**직원들이 뽑은/숨겨놓고 쓰는**" = 내부자 권위 훅 ② "**보이면 무조건 사야되는**" = 품절·긴급 훅 ③ "**A 갈 필요 없는 B**"(코스트코 vs 트레이더스) = 비교 훅 ④ "**TOP3/TOP5**" 숫자 카운트. 우리 9줄 구조(§[3])의 ①고민 훅 자리에 이 네 가지를 돌려 쓴다. **단, 훅에서 특정 유통사 이름을 앞세우지 않는다**(§0 참조) — "직원들이 뽑은"류 권위 훅은 "청소업체가 안 알려주는" 등으로 치환.
  - **비추 레퍼런스:** 살림팩토리·집요정꿀템 등은 10만+ 쇼츠 0개 → 참고 대상에서 제외.
- **타이틀 스타일 레퍼런스 — 온더홈 `@온더홈/shorts` (2026-08-31 추가):** 제품 발굴용이 아니라 **온영상 타이틀 문구 스타일 참고용.** 궁금증 유도형("~의 비밀", "단 2주만", "일본 주부들이 난리난", 의문형 "??")을 2줄 타이틀에 적용한다(§3 타이틀 문구 참조).
- **소스 A-0 — 오늘의굿즈 `@Todays_Goods/shorts` (2026-08-31 신설, 기본 소스):** 사용자가 **카테고리를 지정하지 않으면 이 채널에서 제품을 가져온다.** 방법: `yt-dlp --flat-playlist -J "https://www.youtube.com/@Todays_Goods/shorts"` 로 **최근 30개를 조회수순 정렬** → 상위 영상의 제품을 후보로 삼는다(태그 가능 여부 확인 후 확정). 카테고리를 지정하면(예: "캠핑 꿀템") 기존 소스 A/B + 틱톡 풀 검증으로 발굴한다.
- **소스 B — 네이버 데이터랩 쇼핑인사이트(2026-08-16 확대):** [datalab.naver.com 쇼핑인사이트](https://datalab.naver.com/shoppingInsight/sCategory.naver)에서 **그날그날 인기 검색어(당일 랭킹)** 를 카테고리별로 수집 — `pipeline/1_datalab_keywords.py`가 자동 스크레이핑(Playwright). 대상 카테고리: 디지털/가전·**생활/건강(생활용품·청소용품 핵심)**·스포츠/레저·가구/인테리어·여가/생활편의. 산출물 `work/_datalab/keywords_<YYYYMMDD>.json`
  - ⚠️ **2026-08-28 수정:** ① **기간 설정('직접입력')을 카테고리 선택보다 먼저** 해야 한다 — 순서가 반대면 전 카테고리 0건. ② **시작일=종료일(1일) 구간은 빈 결과**를 돌려준다 → `--days` 최소 2, 기본 **7일(최근 1주)**. 시즌 트렌드는 1주 구간이 당일보다 노이즈가 적다.에서 **해당 제품 카테고리(생활용품·청소용품 우선)** 키워드를 골라 제품 후보로 삼는다.
- **필터:** ① 25–54 남성 적합(생활용품·청소용품은 남녀 공용 실사용 후기 톤 허용) ② 유튜브 쇼핑/쿠팡 등에서 판매·태그 가능 + 리뷰 충분 ③ 최근 3개월 트렌드 또는 데이터랩 당일 상위 랭크.
- **산출물:** `work/<slug>/product.json`
  ```json
  { "product": "차량용 무선 세차기", "ref_url": "...", "ref_views": 320000,
    "category": "차량용품", "shoppable": true, "reason": "...", "keywords": ["차량용품","무선세차기","세차용품"] }
  ```

### [2] 소재 수집  `pipeline/2_download.py` (+ 브라우저)
- Claude가 틱톡에서 **같은 제품·다른 앵글** 영상 3개(기능샷/사용샷/디테일샷)를 탐색 → **중국어 전면 제외 + 영어 자막(자막이면 크기 무관 전면 제외)**. 1회성 짧은 워터마크/브랜드명(작게, 화면 한쪽)만 허용. → 링크 제시 → 사람 확인.
- **2026-08-16 확정 — 실사용 영상 우선:** 브랜드 광고컷·스튜디오 연출컷보다 **실제로 사람이 직접 사용하는 리얼한(UGC성) 영상**을 우선 채택. 검색 시 "review"/"실사용"/"unboxing"보다 **"using"/"real life"/"honest review"/"no talking"/"asmr"** 류 키워드로 진짜 사용 장면 위주 탐색.
- `yt-dlp`로 다운로드 → `work/<slug>/sources/{1,2,3}.mp4`.
- 최종 판별: 콘택트시트로 확인 — **중국어 하나라도 있으면 제외**, **영어 자막은 크기 무관 전면 제외**(작은 브랜드 워터마크만 허용). 연출/광고 느낌이 강한 컷보다 실사용 컷을 우선 채택.

### [2-1] 페르소나 컷  `pipeline/persona.py`  (2026-08-28 신설)

> **채널 고정 페르소나 '란이'.** 매 편 같은 사람이 나와 채널 정체성을 만든다.
> 스펙 단일 출처 = `assets/persona/persona.json`, 얼굴 레퍼런스 = `assets/persona/persona_face_ref.png`.

- **방식: 하이브리드**(2026-08-28 확정). 지정한 나레이션 줄만 페르소나 컷으로 교체하고, 나머지는 기존 틱톡 소재 1-2-3 인터리브를 그대로 쓴다.
  - 이유: 전면 AI로 가면 **실제 제품 묘사가 어긋나** 제휴 전환(수익 90%)이 죽는다. 제품 데모는 실물 소재가 이긴다.
- **노출 규칙: 얼굴 최소 · 손 위주.** ①어텐션 훅에서만 얼굴 1컷, 나머지 페르소나 컷은 손+제품 클로즈업(얼굴 프레임 밖).
- **기본 배치**(9줄 대본 기준, `persona/plan.json`):
  | 줄 | 컷 | 내용 |
  |---|---|---|
  | ① 훅 | `p1` | 얼굴 정면, 제품 들고 말하는 순간 — **유일한 얼굴 컷** |
  | ② 고민 | `p3` | 손 — 문제 상황(예: 냉장고에서 반찬통 꺼내기) |
  | ⑤ 사용법 | `p2` | 손 — 제품 조작(버튼 누르기 등) |
  | ⑨ CTA | `p4` | 손 — 제품을 카메라 쪽으로 제시 + 가리키는 제스처 |

- **생성 방법 (크레딧 0원 경로, 2026-08-28):** 힉스필드는 free 플랜/크레딧 0이라 유료 AI 영상을 못 쓴다.
  → **`codex-image` 로 9:16 스틸을 뽑고 `persona.py` 가 켄번스 모션(줌인/줌아웃 교대 + 미세 팬)을 입혀 클립화**한다.
  - ⚠️ **얼굴 컷은 반드시 `--ref assets/persona/persona_face_ref.png` 를 붙인다.** 텍스트 묘사만으로는 다른 사람이 나온다(2026-08-28 확인).
  - 프롬프트에 고정 요소를 항상 넣는다: 연한 핑크 골지 니트 V넥 · 밝은 화이트톤 한국 가정집 주방 · 자연광 · 짧은 누드네일 · 반지 없음 · **텍스트/자막/워터마크/로고 금지**.
  - 손 컷은 `--ref` 없이도 되지만 **핑크 니트 소매가 손목에 보이게** 써야 같은 사람으로 읽힌다.

- **폴더 규약**
  ```
  work/<slug>/persona/
      p1.png … pN.png     # codex-image 산출 9:16 스틸
      clips/p1.mp4 …       # persona.py clips 가 생성 (3.0s, 켄번스)
      plan.json            # {"1":"p1","2":"p3","5":"p2","9":"p4"}  줄번호→컷
  ```
- **실행**
  ```bash
  python3 pipeline/persona.py <slug> plan     # plan.json 기본값 생성
  python3 pipeline/persona.py <slug> clips    # 스틸 → 모션 클립
  python3 pipeline/4_edit.py <slug> montage   # plan 을 읽어 자동 삽입
  ```
- **구현:** `4_edit.py` 의 `build_montage` 가 `persona.load_plan()` 을 읽어, 각 지정 줄의 **시작 시각과 겹치는 첫 세그먼트**를 페르소나 클립으로 교체한다(겹치면 다음 세그먼트로 밀어냄). 페르소나 컷은 오버스캔 크롭 없이 그대로 채운다.

---

### [3] 대본  `pipeline/3_script.py`

> **v3 구조 (2026-08-31 확정, 온더홈 스타일) — 다음 영상부터 이 구조로 쓴다.** fire_pit 편까지는 v2.
> 근거: 온더홈 `@온더홈/shorts` 조회수 상위 영상 9개 대본(자막) 분석 (130만·130만·72.1만·58.9만·54.6만·44.4만·39.7만·34.5만·27.5만뷰).

**온더홈 상위 대본에서 관찰된 공통 구조 (9개 전수 분석):**
1. **훅 = 제3자 에피소드/감탄 1문장으로 즉발.** "와, 이거 쾌감 대박인데요"(130만) / "엄마가 냉장고 정리 너무 잘했다며 감탄한 꿀템"(130만) / "친구 집에 갔다가 욕실 보고 진짜 충격받았어요"(72.1만) / "이걸로 엄마한테 칭찬받았어요"(27.5만) / "요즘 아마존에서 난리난 타월"(54.6만). 등장 제3자 = 엄마·친구·남편 회전.
2. **발견 스토리가 문제 설명을 대체.** "원인 짚기" 없이 "~했는데 친구가 알려줬어요 / 물어봤더니 이걸 쓴다더라고요"로 문제+해결 발견을 1~2문장에 압축.
3. **시그니처 = "이건 ○○인데(요)" 제품 정의 1문장.** 9개 중 7개 등장 ("이건 슬라이딩 트레이인데", "이건 가구 이동 레일인데요").
4. **사용법은 동작 최소화 표현**: "놓기만 하면 / 끼우기만 하면 / 뿌리고 쓱 밀어주니까".
5. **확장은 "기본이고 ~은 물론 ~까지 / 심지어 ~"** 계단식.
6. **마무리 = 결과 자랑·제3자 재반응 여운**: "이젠 저도 손님한테 칭찬받아요" / "인생템 됐어요" / "다른 건 못 쓰겠대요". 판매 CTA 거의 없음(1개만 댓글 유도).
7. 톤: 존댓말 수다체 "~인데요/~더라고요/~드릴게요", 감탄사 절제, 6~9문장 ~30초. TOP N("두 가지/네 가지") 포맷 2/9.

**나레이션 2트랙 (2026-08-31 확정):** 남 = **필재**(`tc_68257f68bc6e3c161ab5078d`) / 여 = **세희**(`tc_611c3f692fac944dff493a04`) — 편마다 번갈아 쓴다. `script.json` 의 `"voice": "piljae"|"sehee"` 로 지정(`5_tts.py`).
- **제3자 표현을 화자 성별에 맞춘다** (온더홈 상위 대본의 남편/신랑/엄마/친구 패턴 반영): **필재(남) 편 = "와이프가~/아내가~"**, **세희(여) 편 = "남편이~/신랑이~"**. 엄마·친구는 공용.
- 예) 필재: "와이프가 이거 보더니 어디서 샀냐고 물어봤어요" / 세희: "남편이 캠핑 갈 때마다 챙기더라고요".

**v3 9줄 (우리 파이프라인 매핑 — CTA는 수익모델상 유지):**

| 줄 | 역할 | 쓰는 법 | 예시 |
|---|---|---|---|
| ① | **훅: 제3자 에피소드/감탄** | 엄마·친구·남편 반응 or "와~ 대박인데요" or "요즘 난리난" 화제성. 매편 다른 제3자/유형 | "친구 캠핑장 갔다가 화로대 보고 진짜 충격받았어요" |
| ② | **발견 스토리(+짧은 문제)** | "~했는데 친구가 알려줬어요 / 물어봤더니 ~쓴다더라고요" | "짐이 반으로 줄었길래 물어봤더니 이걸 쓴다더라고요" |
| ③ | **제품 정의** | **"이건 ○○인데요"** 고정 시그니처 | "이건 판형으로 접히는 접이식 화로대인데요" |
| ④ | **사용법** | 동작 최소화 "~하기만 하면" | "판 몇 개만 착착 끼우기만 하면" |
| ⑤ | **핵심 효과** | "~니까 ~돼요 / 그 ○○이 대박이에요" | "3초 만에 조립되니까 설치 스트레스가 없어요" |
| ⑥ | **확장** | "기본이고 ~은 물론 ~까지 / 심지어" | "불멍은 기본이고 바베큐에 접으면 가방 수납까지 돼요" |
| ⑦ | **신뢰 보강** | 전문가·화제성·내구성 근거 | "스테인리스라 녹슬 걱정 없이 반영구로 써요" |
| ⑧ | **결과 자랑(제3자 재반응)** | "이젠 ~칭찬받아요 / 인생템 됐어요 / 다른 건 못 쓰겠대요" | "이젠 캠핑 갈 때마다 어디서 샀냐고 물어봐요" |
| ⑨ | **CTA (유지)** | "지금 할인하는데 궁금하면 상품 클릭해보세요!" | 고정 문구 |

- v2 대비 변경: ③원인 짚기 삭제 → **발견 스토리·제품 정의**로 대체, ⑧이 종합이득 나열이 아니라 **제3자 재반응 자랑**, 느낌표 남발 대신 온더홈처럼 담백한 수다체. `sub_text` 형광노랑 마크업·10자 청크 규칙은 동일.

> (구 v2 구조 — 2026-08-24~2026-08-31, fire_pit 편까지 사용:)

- **톤: UGC 수다체 존댓말.** 정보 전달하는 설명조가 아니라 **친한 사람한테 말 걸듯** 쓴다.
  - 핵심 어미: **"~거든요?"**(말 걸기) · **"~거든요"**(원인 설명) · **"~더라구요!"**(경험 전달) · **"~하면 되는건데,"**(다음 줄로 넘기기) · **"완전 바꼈어요!"** · **"완전 꿀템이에요"**
  - **느낌표를 아낀 문장으로 쓰지 않는다.** 감정이 실린 줄(④⑥)은 느낌표로 끝낸다.
  - 평탄한 설명조 문장(예: "~해요.", "~할 수 있어요.")만 이어지면 스크롤을 못 잡는다.

- **⛔ 음성-자막 일치(2026-08-31 확정, 필수):** 화면 자막은 **TTS가 읽는 문장과 반드시 일치**해야 한다. `sub_text` 는 ①형광노랑 `*단어*` 마크업 ②"모~~두" 같은 표기 변형(발음 동일)만 허용 — **단어를 더하거나 빼거나 다른 문장을 쓰는 것 금지.** sub_text 가 없으면 tts 를 그대로 쓴다.
- **⛔ TTS 문장에 넣지 않는 것: "ㅋㅋ" "ㅎㅎ" 웃음, 물결 늘임표("모~~두").** 타입캐스트가 그대로 읽어버려 어색하다.
  → 이런 표현은 **`script.json` 의 `sub_text` 필드**에 쓴다. `4_edit.py` 가 자막을 만들 때 `sub_text` 가 있으면 그걸 쓰고(없으면 `tts`), TTS는 항상 `tts` 만 읽는다. (2026-08-24 구현)

- **분량: 9줄 / 최종 20~25초.** (민지 목소리 기준 나레이션 22~28초 = 1.3배속 후 17~22초. 짧으면 문장을 늘리거나 `4_edit.py` 의 `SPEED` 를 낮춘다.)

- **구조(9줄) — 어텐션 훅 → 내 고민 고백 → 원인 → 제품 전환 → 사용법 → 효과 → 확장 → 이득 → CTA**

  | 줄 | 역할 | 쓰는 법 | 예시 |
  |---|---|---|---|
  | ① | **어텐션 훅** | 끝까지 보게 만드는 한 방. 아래 4유형에서 **매 편 다른 걸** 고른다 | "와~ 신랑이 이거 보더니 반찬 안 버려도 되냐고 깜짝 놀랬어요!" |
  | ② | **내 고민 고백** | "~했었거든요?" 로 내가 겪은 문제를 털어놓는다 | "냉장고에 넣어놔도 반찬이 다 쉬었거든요?" |
  | ③ | **원인 짚기** | "~거든요" 로 왜 그런지 설명 | "뚜껑만 덮어두면 공기가 들어가서 금방 상하거든요" |
  | ④ | **전환 = 제품** | "그런데 이 ○○로 바꾸고 완전 바꼈어요!" | "그런데 이 진공 반찬통으로 바꾸고 완전 바꼈어요!" |
  | ⑤ | **사용법** | 한 동작. "~하면 되는건데," 로 끊어 다음 줄 유도 | "뚜껑 덮고 버튼만 몇 번 누르면 되는건데," |
  | ⑥ | **핵심 효과** | "~더라구요!" 눈에 보이는 결과 | "공기가 빠지면서 신선한 상태로 유지되더라구요!" |
  | ⑦ | **확장 = 다른 데도 됨** | "~까지 모두 ~인데," (자막엔 `sub_text` 로 "모~~두") | "과일이랑 남은 국까지 모두 담을 수 있는데," |
  | ⑧ | **종합 이득** | "~니까 완전 꿀템이에요". 가능하면 구체 숫자 | "음식물도 줄고 음식값까지 아껴지니까 완전 꿀템이에요" |
  | ⑨ | **CTA** | **"지금 할인하는데 궁금하면 상품 클릭해보세요!"** — 유튜브 쇼핑 태그 클릭 유도 | 고정 문구 |

- **① 어텐션 훅 4유형 (2026-08-24 확정) — 편마다 돌려 쓴다. 같은 유형이 연속되지 않게:**
  1. **반응형(제3자 놀람)** — "와~ 남편이 이거 보더니 깜짝 놀랬어요!" / "시어머니가 이거 보더니 어디서 샀냐고 물어봤어요!"
  2. **손해형** — "이거 모르면 진짜 손해에요!" / "이거 모르면 수전 괜히 새로 사서 손해봐요!"
  3. **명령형** — "여러분 제발 ○○ 하세요!" / "여러분 제발 음쓰통에 이것 좀 넣어두세요!"
  4. **숫자 예고형** — "○○하는 꿀팁 세 가지 알려드릴게요!" / "욕실 청소 시간 반으로 줄이는 꿀템 알려드릴게요!"
  - 사회적 증거(제3자 반응)는 **훅(①)이 겸한다.** v1처럼 ⑦에 따로 넣지 않는다.

- **⛔ 금지:** "강추예요"(CTA에 쓰지 않음, 2026-08-19) · "더보기란에 링크 걸어뒀어요"(v2에서 폐기 — 쇼핑 태그 클릭으로 유도) · 매 편 같은 훅 유형 반복 · 화자를 특정 성별/가족관계로 고정하는 반복 표현("신랑이"만 계속 쓰지 말 것)

- **목표는 조회수가 아니라 구매 전환**(수익 90%가 제휴). "이 고민이 있으면 이걸 사면 된다"가 대본에서 분명해야 한다.

- (폐기 이력 — **v1 구조**(2026-08-19~08-22): ①고민훅 ②심화 ③전환 ④사용법 ⑤핵심효과 ⑥부가효과 ⑦사회적증거 ⑧가격 ⑨CTA("더보기란에 링크"). 설명조로 평탄하고 훅이 약해 2026-08-24 폐기. / **11줄 구조**: 28~30초로 길어 폐기.)
- **공식:** "아직도 {문제}를 직접 하세요? 이건 딱 한 번 {제품}으로 하면 {이득}." 
- 첫 2초에 문제/궁금증 무조건 터뜨림.
- **산출물:** `work/<slug>/script.json`
  ```json
  { "title": "아직도 손으로 세차하세요?",
    "lines": [
      {"tts": "아직도 세차할 때 물기를 손으로 닦으세요?", "sub": "손세차 그만", "scene": 1},
      {"tts": "이거 하나면 5초 만에 끝납니다", "sub": "5초면 끝", "scene": 2}
    ] }
  ```
  - `tts` = 나레이션 문장(유라). `sub` = 화면 자막 **8자 이내**. `scene` = 매칭 소스 컷.

### [4] 편집  `pipeline/4_edit.py` (ffmpeg)
- **4-1 컷 짬뽕:** 각 소스 영상 **5등분** → `1-2-3-2-1-3…` 재배열 concat. 원본 순서와 다르게, 소스 3개를 고르게 섞음.
- **중국어 제거:** 상/하단 자막이 대부분 → `crop`으로 제거 후 `scale`로 9:16(1080×1920) 채움. 중앙 워터마크는 blur 박스. **최종 프레임에 간체자 0개**.
- **4-2 타이틀 (상단):** 이미지4 스타일.
  - 색: **노란색(#FFD400 계열) + 흰색** 혼용, **검정 외곽선(굵게)**, 굵은 고딕. 상단 배치.
  - 문구: 클릭 유도 — 문제제기 / 궁금증 / "○○이면 해결!".
- **4-3 자막 (하단 2/3 지점):** 이미지5 위치.
  - **흰색 + 검정 외곽선, 8자 이내**, 빠르게 끊어 배치(가독성·리듬).
- 구현: 자막/타이틀은 **ASS 포맷** 권장(외곽선·타이밍 제어). 규격 상수는 §3.
- **산출물:** `work/<slug>/output.mp4` (1080×1920, ~30fps, H.264+AAC).

### [5] TTS  `pipeline/5_tts.py` (Typecast)
- Typecast API, **'세희(SeHee)' 여성 목소리**(2026-08-31 확정 — 같은 날 문정 tc_68f9c6a72f0f04a417bb136f 시도 후 교체. 이전: 민지 → 현지 → 효은 → 서현). voice_id=`tc_611c3f692fac944dff493a04`, model=`ssfm-v30`, emotion=**happy**. 썰쇼츠 대사 목소리와 동일 화자. ⚠️ 세희는 발화가 빨라 같은 대본이면 총길이가 짧아짐 — 20초 밑돌면 대본을 한 문장 늘린다. `.env`의 `TYPECAST_API_KEY` 사용.
- **문장별 볼륨 평준화(2026-08-31):** 클립마다 loudnorm 측정 → 선형 게인으로 **-16 LUFS** 통일(`normalize_loudness`). 문장 간 볼륨 출렁임 제거. 짧은 클립에 loudnorm 다이내믹 모드를 직접 걸면 문장 안에서 출렁이므로 측정+volume 게인 방식.
- 대본 **문장별 개별 생성** → 각 클립 길이로 **자막·컷 타이밍 자동 계산**(수동 싱크 제거).
- **무음 최소화(2026-08-17 강화)**: 각 클립 앞뒤 무음 공격적 제거(`trim_silence`, pad **0.01**·threshold **-35dB**) + 문장 사이 gap **0.01s**(기본, 이전 0.03s→축소). 늘어짐 방지.
- 산출물: `work/<slug>/tts/line_{n}.wav` + `timing.json`.
- ⚠️ **몽타주는 이 timing.json에 데이터-드리븐으로 정렬** → TTS 재생성 시 반드시 montage부터 다시.

### [6] BGM  `pipeline/6_bgm.py`
- **`bgm/` 폴더**(최상단)에서 선택 → 영상 총 길이에 맞춰 트림/루프.
- **잔잔하게**: volume 0.11 + 완화된 사이드체인 덕킹(threshold0.04·ratio5)으로 나레이션 밑에 은은하게. 끝 1.2s 페이드아웃. `bgm/` 첫 파일 자동 사용.
- **2026-08-24 확정 — BGM 없이 나레이션만 간다.** 사용자 지시. `python3 pipeline/4_edit.py <slug> all --no-bgm` 로 렌더한다(플래그 없으면 기존대로 BGM 삽입).

### [7] 업로드 준비  `pipeline/7_upload_meta.py`
- **제목:** `클릭유도 문구 ｜ 제품명` + 줄바꿈 + `한 줄 설명 #검색키워드`
  - 예) `남친 사줬더니 반응 미쳤다ㅋㅋ｜루피 차량용 디퓨저` / `차에 이거 어때요? #차량용품 #엠비언트라이트`
- **본문:** 3–8줄, 검색 키워드 자연 삽입.
- **해시태그:** 검색 키워드 **5개 이상 필수**.
- **쇼핑 태그 정보:** 제품명 · 검색어 · 스토어 링크 · 가격 → Studio 수동 태깅용 블록.
- 업로드: `uploader/`(ScaleMaker) 재사용 검토. 산출물 `work/<slug>/upload.json`.


### [8] 업로드 실행  `pipeline/8_upload.py`  (2026-08-25 신설)
- **대상 채널: 란빵🐣 `@eggbread0` / `UCxQK4IBAJ1t-dPImmMinCng`** — prd 상단 채널과 동일.
- **하루 1편 / 19:00 KST 예약 공개** (2026-08-25 확정, 이전 20:00에서 변경). 크론 안 쓴다 — 한 번에 올리고 `status.publishAt` 으로 공개만 하루씩 풀린다.
- **쇼핑 태그는 편당 5개 필수** (2026-08-25 확정). 1번=대본의 메인 제품, 2~5번=같은 고민의 대체·확장·소모품.
  후보는 `work/<slug>/upload_draft.json` 의 `shopping_tags` 와 완성본 폴더의 `00_쇼핑태그_5개씩.md`.
  ⚠️ **공개 API 없음 → Studio에서 수동으로 붙인다.**
- **채널 가드**: `shorts/.secrets/yt_token.json` 사용. 업로드 직전 `channels().list(mine=True)` 로 채널 ID 확인, 다르면 중단(@스케일메이커 오업로드 방지).
- **OAuth 동의 "허용" 클릭은 사람이 한다.** 계정 접근 권한 부여라 대신 누르지 않는다.
- 스킬: **`쇼핑쇼츠-업로드`** ("란빵 업로드", "쇼핑쇼츠 업로드" 등으로 발동).


---

## 3. 시각 규격 상수 (확정 — 2026-08-12 사용자 피드백 반영)

| 항목 | 값 | 비고 |
|---|---|---|
| 해상도 | 1080 × 1920 (9:16) | |
| **속도** | **최종 1.3배속** (2026-08-31 1.4 시도 후 원복 — 너무 빠름) | 영상+오디오 동일 배속(setpts=PTS/SPEED, atempo=SPEED) |
| **최종 길이** | **20~25초** (2026-08-19 확정) | 대본 9줄 기준. 넘으면 대본을 줄인다 |
| 타이틀 폰트 | **여기어때 잘난체(Jalnan.ttf)** | 2026-08-31 조선굵은고딕(ChosunKg.ttf) 시도 후 같은 날 잘난체로 원복(사용자 지시). ChosunKg.ttf 는 assets/fonts/ 에 보관 |
| **타이틀 문구** | **궁금증 유도형 2줄** (온더홈 스타일) | 2026-08-31 확정. 단정형("무거운 화로대/이제 3초컷")이 아니라 **비밀·의문형**으로 끝까지 보게 만든다 — 예: "캠핑고수들만/아는 화로대?", "~의 비밀?", "왜 ~할까?". 레퍼런스 = 온더홈 `@온더홈/shorts` |
| 타이틀 자간 | **0px (트래킹 없음)**(`TITLE_LETTER`) | 2026-08-31 -2→-1→0. 숫자로 조절 |
| 타이틀 크기 | **140px** (156의 0.9배) | 2줄, 각 줄 **가로 7자 이내**. 유튜브 쇼츠 안전영역 위해 축소(좌우 여백 ~57px/5.3% 확보) |
| 타이틀 색 | line1 **화이트 #FFFFFF** / line2 **연두 #AAEB3C** | 검정 외곽선 ~12px + 그림자 |
| 타이틀 위치 | 상단, **그라데이션 스크림 밴드 위**(이미지#5 스타일) | 상단 약 520px 검정 그라데이션 |
| 자막 폰트 | **메모먼트 꾹꾹체(MemomentKkukkukk.ttf)** 74px 흰색 | 2026-08-28 변경(Pretendard-ExtraBold→꾹꾹체). 손글씨 톤으로 UGC 수다체와 맞춤 |
| 자막 자간 | **-2px 트래킹**(`SUB_LETTER`, 2026-08-17 추가) | 타이틀과 동일 방식(글자별 렌더) |
| 자막 내용 | **나레이션 문장 전체**를 **쉼표/문장부호에서 우선 끊고**, 그 조각이 10자(공백 제외) 넘을 때만 어절 단위로 추가 분할해 1줄씩 순차 표시 | `_chunk()`(2026-08-17 개정 — 이전엔 10자 고정 어절 분할이라 문장 중간에서 부자연스럽게 끊기는 문제 있었음); 조각별 발화길이 비례 타이밍 |
| 자막 줄수 | **항상 1줄** (조각을 빠르게 넘김) | 원 스펙 "빠르게 끊겨서 짧게" |
| **자막 키워드 강조** | 주요 단어를 **형광 노랑 #FFF200** | 2026-08-31 신설. `script.json` 의 `sub_text` 에 `*단어*` 마크업 → `4_edit.py` `_parse_hl()` 이 그 조각만 노랑 렌더. 공백 포함 구는 `*상품* *클릭*` 처럼 어절별로 따로 마킹(청크 분할 시 마크 유실 방지) |
| 자막 외곽선 | **검정 4px** + 그림자 (`SUB_STROKE`) | 2026-08-28 8→4 로 얇게 | |
| 자막 위치 | y=0.655H (하단 약 1/3, 중앙 정렬) | 이미지#5 |

> 폰트는 `assets/fonts/`의 상업사용 가능(OFL) 폰트만 사용. 타이틀·자막 모두 **PIL로 PNG 렌더 후 ffmpeg overlay** 합성(§6 ffmpeg 주의).
> **스킬 반영:** 이 §3 규격이 `/쇼핑쇼츠` 스킬의 시각 규격 단일 출처. 코드 구현 = `pipeline/4_edit.py`.

---

## 4. 폴더 구조

```
shorts/
├─ prd.md                     # 이 문서 (단일 출처)
├─ requirements.txt
├─ .env.example / .env        # TYPECAST_API_KEY 등 (.env는 커밋 금지)
├─ pipeline/
│   ├─ 1_discover.py          # 제품 발굴(브라우저 병행)
│   ├─ 2_download.py          # 틱톡 다운로드(yt-dlp)
│   ├─ 3_script.py            # 대본 생성
│   ├─ 4_edit.py              # ffmpeg 편집(컷섞기·크롭·타이틀·자막)
│   ├─ 5_tts.py               # Typecast 유라 TTS
│   ├─ 6_bgm.py               # BGM 믹스·덕킹
│   ├─ 7_upload_meta.py       # 제목/본문/해시태그/쇼핑태그
│   └─ common.py              # 공통(경로·ffmpeg 헬퍼)
├─ assets/
│   ├─ bgm -> ../bgm          # 최상단 bgm/ 심볼릭 링크
│   └─ fonts/                 # 타이틀·자막 폰트
├─ bgm/                       # 배경음악 (사용자가 채움)
└─ work/<제품slug>/           # 제품별 작업 폴더
    ├─ product.json  sources/  script.json  tts/  output.mp4  upload.json
```

## 5. 스택 / 의존성
- Python 3, **ffmpeg**(설치됨 8.1.2), **yt-dlp**(설치 필요), Typecast REST API, Claude(대본·메타).
- 브라우저 탐색: chrome-mcp-stdio MCP.

## 6. 현재 상태 (2026-08-12)
- [x] 결정 확정: 스킬 반자동 / 브라우저 탐색 / 유튜브 쇼핑 태그
- [x] 폴더 스캐폴드 + prd.md
- [x] `assets/fonts/` 폰트 확보 — BlackHanSans(타이틀) + Pretendard(자막), 둘 다 OFL 상업가능
- [x] `.env` TYPECAST_API_KEY + 효은 voice_id=`tc_691d49ccc47926d741f15913`(ssfm-v30)
- [x] yt-dlp 설치(`python3 -m yt_dlp`) — 단, TikTok은 tikwm API로 다운로드(쿠키불필요)
- [x] pipeline 구현: 2_download / 4_edit(montage·text·audio) / 5_tts / 7_upload_meta
- [x] 시각 규격 확정: 타이틀 BlackHanSans 112px(흰+노랑#FFD400, 검정외곽선13, 상단 스크림) / 자막 Pretendard-ExtraBold 74px(흰, 검정외곽선9, y=0.63H)
- [x] **파일럿 1편 완성**: work/ambient_light/output.mp4 (차량용 엠비언트 라이트, 21.4s, 1080×1920)
- [x] **스킬 생성**: `~/.claude/skills/쇼핑쇼츠/SKILL.md` (오케스트레이션, 단일출처=이 prd)
- [x] 자막 방식 확정: 나레이션 10자 이내 1줄씩 순차(`_chunk`), 타이틀 흰+연두, 1.3배속
- [ ] `bgm/` 배경음악 넣고 `python3 pipeline/4_edit.py ambient_light audio` 재실행 → 덕킹 자동
- [ ] 파일럿 업로드 후 반응 검증 → 제품만 바꿔 반복 생산

### ffmpeg 주의
Homebrew ffmpeg 8.1.2에 libass/drawtext 없음 → **텍스트는 PIL로 PNG 렌더 후 overlay**로 합성(4_edit.py). 이 방식이 이미지#4·#5 디자인 픽셀 재현에 더 정확.

## 6-1. 확정 사항 (2026-08-13)
- **대본 = 11줄 바이럴 구조**(레퍼런스 5만뷰+ 분석) 확정 · **1.3배속** · **BGM 잔잔(vol0.11+덕킹완화)** 확정.
- **소재 규칙(2026-08-16 개정): 중국어 전면 제외 + 영어 자막 크기 무관 전면 제외**(1회성 짧은 브랜드 워터마크만 허용) + **실사용(UGC성) 영상 우선**(연출·광고 느낌 컷보다 실제 사용 장면 채택).

## 7. 파일럿 제작 로그 (ambient_light)
- 제품: 남자마켓 인기순 상위(266만·195만 2회 랭크인)에서 엠비언트라이트 선정
- 소재 3개(tikwm): c4 현대(무자막 설치+리빌) / c3 아우디·벤츠(무자막) / c2 혼다(5s+ 무자막, 리모컨) — 전부 중국어 없음
- 편집: 11세그먼트 1-2-3 인터리브 + 8% 오버스캔크롭(엣지 워터마크 제거), 21.38s
- TTS 유라 7문장 → 21.38s, 문장별 타이밍으로 자막·컷 싱크
- 산출: product.json / script.json / tts/ / montage.mp4 / overlays/ / video_noaudio.mp4 / output.mp4 / upload.txt

## 8. 2차 제작 로그 (2026-08-14, 8편 추가)

**제품 발굴**: 레퍼런스 채널(쇼핑남자·남자마켓·hommster·살룸연구소) 채널 내부검색 API로 실제 조회수 10만+ 근거를 재검증. 처음 제안한 8개 후보 중 7개는 근거를 못 찾아, 실증거된 대체 제품으로 전량 교체 확정: 차량용 무선 에어건(air_blower)·차량용 트렁크 정리매트(cargo_mat)·자동 알약/영양제 디스펜서(pill_dispenser)·차량용 UV 인디케이터(uv_duck)·포켓몬 미니가습기(pokemon_humidifier)·포켓몬 카드/피규어(pokemon_figure)·캠핑용 미니난로(camping_stove)·차량용 암레스트 쿠션(armrest_cushion).

**⚠️ 신규 발견 함정 — "지속 캡션" 소스**: 8개 중 6개에서 1차 제작 시 놓친 문제 발견 — 소스 영상에 **문장 단위 캡션이 영상 내내 지속되는 경우**(한국어 리뷰 자막, 브랜드 타이틀 카드, TikTok 자동자막 등)가 있으면 렌더링되는 한글 자막/타이틀과 겹쳐 이중 캡션처럼 보인다. 기존 "중국어 제외·큰 영어자막 제외" 기준만으로는 이걸 못 걸러낸다.
- **강화된 소스 검증 기준**: 1회성 짧은 워터마크/브랜드명(작게, 화면 한쪽)만 허용. **문장 단위 캡션이 반복·지속되면 크기와 무관하게 배제.** 차량 대시보드/내비게이션 화면 등 배경에 중국어가 우연히 찍힌 경우도 배제 대상.
- **검색 팁**: "no talking" / "asmr" / "silent" 키워드를 붙이면 내레이션(및 그에 딸린 자동자막)이 없는 클립을 찾기 쉽다. 말하는 사람 얼굴이 클로즈업되는 "토크형" 콘텐츠는 자막이 달릴 확률이 높아 우선 배제.
- **검수 방법**: 프레임 2~3장 샘플로는 지속 캡션을 놓치기 쉽다. `ffmpeg -vf "fps=1/2,tile=4x4"` 로 콘택트시트를 만들어 소스 전체 구간을 한 번에 검수하는 게 안전.

**산출**: 8개 output.mp4 최종본은 `20260814_쇼핑쇼츠영상/` 폴더에 별도 보관.

**7_upload_meta.py 버그 수정(2026-08-14)**: 기존 스크립트가 제품과 무관하게 ambient_light 문구를 하드코딩하고 있었음(9개 제품이 실제로는 손으로 upload.txt를 작성했던 이유). `work/<slug>/upload_draft.json`(title/body_lines/hashtags/search_query/price_range/schedule)을 입력받아 product.json과 합쳐 쇼핑태그 블록까지 포함한 upload.json/upload.txt를 생성하도록 재작성.

## 9. 3차 제작 로그 (2026-08-18, 청소용품 5편)

**레퍼런스 채널 추가**: **단순현 `@dansunhyun/shorts`** — 청소·살림 전문, 숏츠 780편, 상위권이 수백만~1천만뷰. 생활용품·청소용품 소재 발굴의 1순위 소스가 됐다(§2[1] 소스 A에 반영). 조회수 근거는 `yt-dlp --flat-playlist -J "https://www.youtube.com/@<채널>/shorts"` 로 전편 view_count 를 한 번에 뽑아 정렬하면 가장 빠르고 정확하다(채널 내부검색 API보다 간편).

**제작 5편**(전부 @dansunhyun 조회수 근거):
| slug | 제품 | 근거(조회수) |
|---|---|---|
| toilet_gel | 변기 청소 스탬프 젤 | 480만·110만·23.9만 |
| drain_brush | 배수구 청소 브러시 | 76.6만·7.3만 |
| glass_cleaner | 유리·거울 물때 클리너+스퀴지 | 100만·5.9만 |
| spin_mop | 회전 물걸레 스핀 밀대 | 520만·14만 |
| washer_cleaner | 세탁조 클리너 | 260만·47.1만·12.6만 |

**신규 스크립트 `pipeline/2_search.py`**: tikwm `feed/search` API로 틱톡 후보를 키워드 검색해 `길이/조회수/URL/제목`을 뽑는다(브라우저 불필요). ⚠️ 엔드포인트는 **`/api/feed/search/` (끝 슬래시 필수) + GET** 이어야 한다 — 슬래시 없거나 POST면 Cloudflare 챌린지 HTML이 돌아온다.

**⚠️ 소재 확보 난이도는 카테고리마다 크게 다르다 (신규 발견)**: 후보 33+22개를 받아 콘택트시트로 검수한 결과 통과율이 제품별로 극단적으로 갈렸다.
- 통과 쉬움: **세탁조 클리너(5/5)**, **유리·거울(6/8)**, 스핀몹(4/11) — ASMR·"satisfying" 계열이 발달한 무자막 니치.
- 통과 불가: **욕실 곰팡이 제거 젤 — 후보 12개 중 11개가 지속 영문자막**(제품 설명형 콘텐츠라 캡션이 기본). 3개 검색 앵글을 써도 소재를 못 채워 **제품 자체를 유리·거울 클리너로 교체**했다.
- **교훈:** 제품을 확정하기 전에 `2_search.py` 로 후보 풀을 먼저 훑어 "무자막 ASMR 계열이 존재하는 제품인가"를 확인할 것. 설명형·튜토리얼형 니치(곰팡이·세제 사용법 등)는 소재가 안 나온다.

**⚠️ 콘택트시트 6프레임으로는 짧은 캡션을 놓친다**: drain_brush 소스2에서 6프레임 시트는 통과했으나 최종 프리뷰에 "Wait for the pull" 영문 캡션이 잡혔다. 24프레임 조밀 샘플로 재검수해 캡션 구간(2~6s)을 확인하고 `-ss 8` 로 앞부분을 잘라내 재렌더했다. → **최종 output.mp4도 8~16프레임 프리뷰 시트로 반드시 재검수**할 것(소스 시트 통과 ≠ 최종 통과).

**산출**: `20260818_청소용품쇼츠/` 에 5편 mp4 + upload.txt 보관. 길이 27.8~30.3s, 1080×1920, 1.3배속.

## 10. 대본 개정 (2026-08-19)

3차 5편(청소용품)을 사용자 피드백으로 전면 재작성했다.
1. **"강추예요" 제거** — 모든 편의 마지막 줄이 "지금 확인해보세요, 강추예요"로 똑같아 상투적. CTA는 "더보기란에 링크 걸어뒀어요"로 교체.
2. **11줄 → 9줄, 28~30초 → 20~25초** — 길어서 이탈. 문장 자체도 짧게.
3. **구조 재정의** — 기능 나열형에서 **"고민 훅 → 해결책으로 제품 제안 → 구매 유도"** 로. §2[3] 참조.

재렌더 결과: toilet_gel 22.4s / drain_brush 21.6s / glass_cleaner 20.0s / spin_mop 22.0s / washer_cleaner 20.9s.
⚠️ 대본을 고치면 **5_tts → montage → text → audio** 전 단계를 다시 돌려야 한다(타이밍이 timing.json 의존).
