# 쇼핑쇼츠 자동화 — PRD (단일 출처)

> 이 문서가 규격의 **단일 출처(Single Source of Truth)**다. 스킬/스크립트는 이 문서를 따른다.
> 채널: https://www.youtube.com/channel/UCxQK4IBAJ1t-dPImmMinCng

---

## 0. 목표와 전략

**목표:** 이 채널에 쇼핑쇼츠를 꾸준히 업로드해 수익화.

**채널 데이터 기반 전략 (지난 28일 / 6개월):**

| 지표 | 값 | 방향 |
|---|---|---|
| 성별 | 남 74.6% / 여 25.4% | **남성향 제품** |
| 연령 | 25–34(37.2%) · 35–44(22.5%) · 45–54(18.2%) = 약 78% | **구매력 있는 25–54 남성** |
| 수익원 | 제휴 71,015원 ≫ Shorts광고 15,143원 ≫ 보기광고 24원 | **핵심은 제휴 전환** |
| 수익 추세 | 6월 24만 → 8월 2.4만 (하락) | 개편 필요 |

**핵심 결론:** 조회수(광고)보다 **제휴 전환**이 수익의 90%. 제품 선정 1순위 기준 = "25–54 남성이 실제 지갑 여는 + 유튜브 쇼핑으로 태그 가능한 제품".

**타겟 카테고리:** 차량용품 · 생활/청소 꿀템 · 공구/DIY · 가전 소품 · 자기관리(면도·바디) · 캠핑/차박.

**수익화 방식(확정):** **유튜브 쇼핑 태그** — 각 쇼츠에 제품을 직접 태그한다.
- 태깅은 YouTube Studio에서 **수동**(공개 태깅 API 없음). 파이프라인은 "태그용 제품 정보 블록"(제품명·검색어·스토어 링크·가격)을 산출해 사람이 Studio에서 붙이게 한다.
- 전제: 채널이 **YouTube 쇼핑 제휴 프로그램** 자격을 갖춰야 함(이미 제휴 수익 발생 중이므로 충족 가정, 미충족 시 [7]에서 안내).

**⚠️ 저작권 리스크 (반드시 인지):** 틱톡 영상 재사용은 "재사용된 콘텐츠(reused content)"·저작권 정책에 걸릴 수 있음. 최근 수익 하락과 연관 가능성 있음. → [4] 편집에서 **컷 재배열 + 크롭/확대 + 타이틀/자막/TTS/BGM 레이어**로 최대한 변형을 준다. 완전 면제는 아님.

---

## 1. 실행 형태 (확정)

- **스킬 반자동** (`/쇼핑쇼츠`, 이름 확정 예정). 다른 프로젝트(scalemaker-yt, cardnews)와 동일 패턴.
- **사람이 컨펌하는 지점:** ① 제품 선정 ② 틱톡 소재 3개 최종 선택 ③ 대본 ④ 최종 영상 프리뷰 ⑤ 업로드 메타.
- **자동으로 도는 지점:** 편집(컷섞기·크롭·타이틀·자막 합성)·TTS·BGM·메타 초안.
- **틱톡 소재 확보:** Claude가 브라우저(claude-in-chrome)로 틱톡에서 중국어 없는 후보를 탐색해 링크 제시 → 사람 확인 → 다운로드.

---

## 2. 파이프라인 (7단계)

```
[1] 제품 발굴 → [2] 소재 수집(틱톡 3개) → [3] 대본 → [4] 편집
     → [5] TTS(효은) → [6] BGM → [7] 업로드 준비(+쇼핑 태그 정보)
```

### [1] 제품 발굴  `pipeline/1_discover.py` (+ 브라우저)
- **소스:** 레퍼런스 5채널의 숏츠 중 **조회수 10만+**.
  - 쇼핑남자 `@쇼핑남자` · 남자마켓 `@남자마켓/shorts` · hommster `@hommster/shorts` · 살룸연구소 `@살룸연구소/shorts` · TJ홈스토어 `@TJ홈스토어/shorts`
- **필터:** ① 25–54 남성 적합 ② 유튜브 쇼핑/쿠팡 등에서 판매·태그 가능 + 리뷰 충분 ③ 최근 3개월 트렌드.
- **산출물:** `work/<slug>/product.json`
  ```json
  { "product": "차량용 무선 세차기", "ref_url": "...", "ref_views": 320000,
    "category": "차량용품", "shoppable": true, "reason": "...", "keywords": ["차량용품","무선세차기","세차용품"] }
  ```

### [2] 소재 수집  `pipeline/2_download.py` (+ 브라우저)
- Claude가 틱톡에서 **같은 제품·다른 앵글** 영상 3개(기능샷/사용샷/디테일샷)를 탐색 → **중국어는 전면 제외**. **영어는 작은 자막은 허용**하되 **화면을 크게 차지하는 영어 타이틀/큰 자막은 제외**. 워터마크는 작으면 허용. → 링크 제시 → 사람 확인.
- `yt-dlp`로 다운로드 → `work/<slug>/sources/{1,2,3}.mp4`.
- 최종 판별: 콘택트시트로 확인 — **중국어 하나라도 있으면 제외**, **크게 들어간 영어 타이틀/자막 있으면 제외**. 작은 영어 자막은 허용.

### [3] 대본  `pipeline/3_script.py`
- **톤(기본): UGC(실사용 후기)**, **친근한 존댓말**("~거든요","~잖아요","~해보세요","강추예요"), ㅋㅋ 톤 살짝.
- **분량: 11줄** (7줄은 너무 짧음 — 레퍼런스 5만뷰+ 분석 결과 심화·기능나열·사회적증거가 길이의 핵심).
- **레퍼런스 5만뷰+ 검증 구조(11줄):** ①도발 훅("~해본 사람 저요","절대 ~하지마") ②공감(상황 구체화) ③심화(손해·불편 강조) ④반전("근데 이거 하나면") ⑤사용법(초간단) ⑥⑦⑧기능 2~3개 ⑨**사회적 증거(제3자 반응)=바이럴 핵심**("친구가 어디서 샀냐고 난리") ⑩혜택/가격(구체 숫자, "몇천원으로 십만원 막음") ⑪CTA(긴급 "지금 확인").
- 훅 유형: 반응형/도발·금지형/궁금증형/후회·긴급형/상황형 (살룸연구소 1500만, 남자마켓 380만 등에서 반복 검증).
- **공식:** "아직도 {문제}를 직접 하세요? 이건 딱 한 번 {제품}으로 하면 {이득}." 
- 첫 2초에 문제/궁금증 무조건 터뜨림.
- **산출물:** `work/<slug>/script.json`
  ```json
  { "title": "아직도 손으로 세차하세요?",
    "lines": [
      {"tts": "아직도 세차할 때 물기를 손으로 닦으세요?", "sub": "손세차 그만", "scene": 1},
      {"tts": "이거 하나면 5초 만에 끝납니다", "sub": "5초면 끝", "scene": 2}
    ] }
  ```
  - `tts` = 나레이션 문장(유라). `sub` = 화면 자막 **8자 이내**. `scene` = 매칭 소스 컷.

### [4] 편집  `pipeline/4_edit.py` (ffmpeg)
- **4-1 컷 짬뽕:** 각 소스 영상 **5등분** → `1-2-3-2-1-3…` 재배열 concat. 원본 순서와 다르게, 소스 3개를 고르게 섞음.
- **중국어 제거:** 상/하단 자막이 대부분 → `crop`으로 제거 후 `scale`로 9:16(1080×1920) 채움. 중앙 워터마크는 blur 박스. **최종 프레임에 간체자 0개**.
- **4-2 타이틀 (상단):** 이미지4 스타일.
  - 색: **노란색(#FFD400 계열) + 흰색** 혼용, **검정 외곽선(굵게)**, 굵은 고딕. 상단 배치.
  - 문구: 클릭 유도 — 문제제기 / 궁금증 / "○○이면 해결!".
- **4-3 자막 (하단 2/3 지점):** 이미지5 위치.
  - **흰색 + 검정 외곽선, 8자 이내**, 빠르게 끊어 배치(가독성·리듬).
- 구현: 자막/타이틀은 **ASS 포맷** 권장(외곽선·타이밍 제어). 규격 상수는 §3.
- **산출물:** `work/<slug>/output.mp4` (1080×1920, ~30fps, H.264+AAC).

### [5] TTS  `pipeline/5_tts.py` (Typecast)
- Typecast API, **'효은' 목소리**(emotion=**happy**, 텐션↑). `.env`의 `TYPECAST_API_KEY` 사용.
- 대본 **문장별 개별 생성** → 각 클립 길이로 **자막·컷 타이밍 자동 계산**(수동 싱크 제거).
- **무음 최소화**: 각 클립 앞뒤 무음 제거(`trim_silence`, pad 0.04) + 문장 사이 gap **0.03s**(기본). 늘어짐 방지.
- 산출물: `work/<slug>/tts/line_{n}.wav` + `timing.json`.
- ⚠️ **몽타주는 이 timing.json에 데이터-드리븐으로 정렬** → TTS 재생성 시 반드시 montage부터 다시.

### [6] BGM  `pipeline/6_bgm.py`
- **`bgm/` 폴더**(최상단)에서 선택 → 영상 총 길이에 맞춰 트림/루프.
- **잔잔하게**: volume 0.11 + 완화된 사이드체인 덕킹(threshold0.04·ratio5)으로 나레이션 밑에 은은하게. 끝 1.2s 페이드아웃. `bgm/` 첫 파일 자동 사용.

### [7] 업로드 준비  `pipeline/7_upload_meta.py`
- **제목:** `클릭유도 문구 ｜ 제품명` + 줄바꿈 + `한 줄 설명 #검색키워드`
  - 예) `남친 사줬더니 반응 미쳤다ㅋㅋ｜루피 차량용 디퓨저` / `차에 이거 어때요? #차량용품 #엠비언트라이트`
- **본문:** 3–8줄, 검색 키워드 자연 삽입.
- **해시태그:** 검색 키워드 **5개 이상 필수**.
- **쇼핑 태그 정보:** 제품명 · 검색어 · 스토어 링크 · 가격 → Studio 수동 태깅용 블록.
- 업로드: `uploader/`(ScaleMaker) 재사용 검토. 산출물 `work/<slug>/upload.json`.

---

## 3. 시각 규격 상수 (확정 — 2026-08-12 사용자 피드백 반영)

| 항목 | 값 | 비고 |
|---|---|---|
| 해상도 | 1080 × 1920 (9:16) | |
| **속도** | **최종 1.3배속** | 영상+오디오 동일 배속(setpts=PTS/1.3, atempo=1.3) |
| 타이틀 폰트 | **여기어때 잘난체(Jalnan.ttf)** | (BlackHanSans→Pretendard→Jua→도현→**잘난체** 최종) |
| 타이틀 자간 | **-2px 트래킹**(`TITLE_LETTER`) | 숫자로 조절 |
| 타이틀 크기 | **140px** (156의 0.9배) | 2줄, 각 줄 **가로 7자 이내**. 유튜브 쇼츠 안전영역 위해 축소(좌우 여백 ~57px/5.3% 확보) |
| 타이틀 색 | line1 **화이트 #FFFFFF** / line2 **연두 #AAEB3C** | 검정 외곽선 ~12px + 그림자 |
| 타이틀 위치 | 상단, **그라데이션 스크림 밴드 위**(이미지#5 스타일) | 상단 약 520px 검정 그라데이션 |
| 자막 폰트 | Pretendard-ExtraBold 74px 흰색 | |
| 자막 내용 | **나레이션 문장 전체**를 **10자 이내(공백 제외) 조각으로 어절 끊어 1줄씩 순차 표시** | `_chunk()`; 조각별 발화길이 비례 타이밍 |
| 자막 줄수 | **항상 1줄** (조각을 빠르게 넘김) | 원 스펙 "빠르게 끊겨서 짧게" |
| 자막 외곽선 | 검정 8px + 그림자 | |
| 자막 위치 | y=0.655H (하단 약 1/3, 중앙 정렬) | 이미지#5 |

> 폰트는 `assets/fonts/`의 상업사용 가능(OFL) 폰트만 사용. 타이틀·자막 모두 **PIL로 PNG 렌더 후 ffmpeg overlay** 합성(§6 ffmpeg 주의).
> **스킬 반영:** 이 §3 규격이 `/쇼핑쇼츠` 스킬의 시각 규격 단일 출처. 코드 구현 = `pipeline/4_edit.py`.

---

## 4. 폴더 구조

```
shorts/
├─ prd.md                     # 이 문서 (단일 출처)
├─ requirements.txt
├─ .env.example / .env        # TYPECAST_API_KEY 등 (.env는 커밋 금지)
├─ pipeline/
│   ├─ 1_discover.py          # 제품 발굴(브라우저 병행)
│   ├─ 2_download.py          # 틱톡 다운로드(yt-dlp)
│   ├─ 3_script.py            # 대본 생성
│   ├─ 4_edit.py              # ffmpeg 편집(컷섞기·크롭·타이틀·자막)
│   ├─ 5_tts.py               # Typecast 유라 TTS
│   ├─ 6_bgm.py               # BGM 믹스·덕킹
│   ├─ 7_upload_meta.py       # 제목/본문/해시태그/쇼핑태그
│   └─ common.py              # 공통(경로·ffmpeg 헬퍼)
├─ assets/
│   ├─ bgm -> ../bgm          # 최상단 bgm/ 심볼릭 링크
│   └─ fonts/                 # 타이틀·자막 폰트
├─ bgm/                       # 배경음악 (사용자가 채움)
└─ work/<제품slug>/           # 제품별 작업 폴더
    ├─ product.json  sources/  script.json  tts/  output.mp4  upload.json
```

## 5. 스택 / 의존성
- Python 3, **ffmpeg**(설치됨 8.1.2), **yt-dlp**(설치 필요), Typecast REST API, Claude(대본·메타).
- 브라우저 탐색: claude-in-chrome MCP.

## 6. 현재 상태 (2026-08-12)
- [x] 결정 확정: 스킬 반자동 / 브라우저 탐색 / 유튜브 쇼핑 태그
- [x] 폴더 스캐폴드 + prd.md
- [x] `assets/fonts/` 폰트 확보 — BlackHanSans(타이틀) + Pretendard(자막), 둘 다 OFL 상업가능
- [x] `.env` TYPECAST_API_KEY + 효은 voice_id=`tc_691d49ccc47926d741f15913`(ssfm-v30)
- [x] yt-dlp 설치(`python3 -m yt_dlp`) — 단, TikTok은 tikwm API로 다운로드(쿠키불필요)
- [x] pipeline 구현: 2_download / 4_edit(montage·text·audio) / 5_tts / 7_upload_meta
- [x] 시각 규격 확정: 타이틀 BlackHanSans 112px(흰+노랑#FFD400, 검정외곽선13, 상단 스크림) / 자막 Pretendard-ExtraBold 74px(흰, 검정외곽선9, y=0.63H)
- [x] **파일럿 1편 완성**: work/ambient_light/output.mp4 (차량용 엠비언트 라이트, 21.4s, 1080×1920)
- [x] **스킬 생성**: `~/.claude/skills/쇼핑쇼츠/SKILL.md` (오케스트레이션, 단일출처=이 prd)
- [x] 자막 방식 확정: 나레이션 10자 이내 1줄씩 순차(`_chunk`), 타이틀 흰+연두, 1.3배속
- [ ] `bgm/` 배경음악 넣고 `python3 pipeline/4_edit.py ambient_light audio` 재실행 → 덕킹 자동
- [ ] 파일럿 업로드 후 반응 검증 → 제품만 바꿔 반복 생산

### ffmpeg 주의
Homebrew ffmpeg 8.1.2에 libass/drawtext 없음 → **텍스트는 PIL로 PNG 렌더 후 overlay**로 합성(4_edit.py). 이 방식이 이미지#4·#5 디자인 픽셀 재현에 더 정확.

## 6-1. 확정 사항 (2026-08-13)
- **대본 = 11줄 바이럴 구조**(레퍼런스 5만뷰+ 분석) 확정 · **1.3배속** · **BGM 잔잔(vol0.11+덕킹완화)** 확정.
- **소재 규칙: 중국어 전면 제외 + 크게 들어간 영어 타이틀/자막 제외**(작은 영어 자막·작은 워터마크는 허용).

## 7. 파일럿 제작 로그 (ambient_light)
- 제품: 남자마켓 인기순 상위(266만·195만 2회 랭크인)에서 엠비언트라이트 선정
- 소재 3개(tikwm): c4 현대(무자막 설치+리빌) / c3 아우디·벤츠(무자막) / c2 혼다(5s+ 무자막, 리모컨) — 전부 중국어 없음
- 편집: 11세그먼트 1-2-3 인터리브 + 8% 오버스캔크롭(엣지 워터마크 제거), 21.38s
- TTS 유라 7문장 → 21.38s, 문장별 타이밍으로 자막·컷 싱크
- 산출: product.json / script.json / tts/ / montage.mp4 / overlays/ / video_noaudio.mp4 / output.mp4 / upload.txt

## 8. 2차 제작 로그 (2026-08-14, 8편 추가)

**제품 발굴**: 레퍼런스 채널(쇼핑남자·남자마켓·hommster·살룸연구소) 채널 내부검색 API로 실제 조회수 10만+ 근거를 재검증. 처음 제안한 8개 후보 중 7개는 근거를 못 찾아, 실증거된 대체 제품으로 전량 교체 확정: 차량용 무선 에어건(air_blower)·차량용 트렁크 정리매트(cargo_mat)·자동 알약/영양제 디스펜서(pill_dispenser)·차량용 UV 인디케이터(uv_duck)·포켓몬 미니가습기(pokemon_humidifier)·포켓몬 카드/피규어(pokemon_figure)·캠핑용 미니난로(camping_stove)·차량용 암레스트 쿠션(armrest_cushion).

**⚠️ 신규 발견 함정 — "지속 캡션" 소스**: 8개 중 6개에서 1차 제작 시 놓친 문제 발견 — 소스 영상에 **문장 단위 캡션이 영상 내내 지속되는 경우**(한국어 리뷰 자막, 브랜드 타이틀 카드, TikTok 자동자막 등)가 있으면 렌더링되는 한글 자막/타이틀과 겹쳐 이중 캡션처럼 보인다. 기존 "중국어 제외·큰 영어자막 제외" 기준만으로는 이걸 못 걸러낸다.
- **강화된 소스 검증 기준**: 1회성 짧은 워터마크/브랜드명(작게, 화면 한쪽)만 허용. **문장 단위 캡션이 반복·지속되면 크기와 무관하게 배제.** 차량 대시보드/내비게이션 화면 등 배경에 중국어가 우연히 찍힌 경우도 배제 대상.
- **검색 팁**: "no talking" / "asmr" / "silent" 키워드를 붙이면 내레이션(및 그에 딸린 자동자막)이 없는 클립을 찾기 쉽다. 말하는 사람 얼굴이 클로즈업되는 "토크형" 콘텐츠는 자막이 달릴 확률이 높아 우선 배제.
- **검수 방법**: 프레임 2~3장 샘플로는 지속 캡션을 놓치기 쉽다. `ffmpeg -vf "fps=1/2,tile=4x4"` 로 콘택트시트를 만들어 소스 전체 구간을 한 번에 검수하는 게 안전.

**산출**: 8개 output.mp4 최종본은 `20260814_쇼핑쇼츠영상/` 폴더에 별도 보관.

**7_upload_meta.py 버그 수정**: 기존 스크립트가 제품과 무관하게 ambient_light 문구를 하드코딩하고 있었음(9개 제품이 실제로는 손으로 upload.txt를 작성했던 이유). `work/<slug>/upload_draft.json`(title/body_lines/hashtags/search_query/price_range/schedule)을 입력받아 product.json과 합쳐 쇼핑태그 블록까지 포함한 upload.json/upload.txt를 생성하도록 재작성.
